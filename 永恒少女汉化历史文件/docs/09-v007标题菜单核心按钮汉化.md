# 《永恒少女》NS 汉化阶段 09：v007 标题菜单核心按钮汉化

## 1. 基线

本阶段从已通过实机缺字复测的：

```text
v006a-settings-images-fontfix
```

重新组装，不覆盖 v006a。

v006a 中已确认的设置界面图片、动态 UI、剧情与运行时 TTC 全部保持原样。

---

## 2. 标题菜单资源绑定

`System/title_menu.spm` 为 `SPM VER-2.02`，明确引用：

```text
title_btn0001.png ～ title_btn0009.png
title_btn0101.png ～ title_btn0109.png
title_btn0208.png
```

公开 Switch 体体验版标题截图为 1200x675，对本地 284x84 按钮素材按 0.625 比例执行模板匹配，得到：

```text
title_btn0001 -> x=316,y=450 -> はじめから -> 从头开始
title_btn0002 -> x=511,y=450 -> つづきから -> 继续
title_btn0003 -> x=706,y=450 -> 環境設定   -> 环境设置
```

三项匹配位置与公开截图的左→右顺序完全一致。

公开完整标题画面中：

```text
title_btn0004 -> 第二行 x=412,y=522
```

并与正式版菜单资料中的第 4 项 `鑑賞` 对应，因此译为：

```text
鉴赏
```

映射表已更新：

```text
03_text/ui/title-system-image-mapping-v1.tsv
```

`0005～0009 / 0105～0109 / 0208` 仍未达到零猜测绑定标准，本版继续安全保留原图。

---

## 3. 状态族

本阶段同步修改普通态和同编号高亮/备用态：

```text
title_btn0001.png   从头开始
title_btn0101.png   从头开始

title_btn0002.png   继续
title_btn0102.png   继续

title_btn0003.png   环境设置
title_btn0103.png   环境设置

title_btn0004.png   鉴赏
title_btn0104.png   鉴赏
```

总计：

```text
8 PNG
```

---

## 4. title_btn0003 特殊图集

`title_btn0003.png` 实际尺寸：

```text
672x672
```

但 `title_menu.spm` 实际只使用左上约：

```text
284x84
```

生成脚本严格只修改左上按钮区域，其余 atlas 内容保持字节级视觉数据不变。

最终 QA：

```text
title_btn0003 outside_button_changed = 0
```

---

## 5. 图片生成

脚本：

```text
04_scripts/localize_title_buttons.py
```

输出：

```text
03_text/ui/rendered-v007-title/
```

处理方式：

```text
只在标题文字中央区域建立白字 mask
OpenCV inpaint 清除原日文标签
保持 Alpha 通道逐像素不变
使用微软雅黑烘焙中文
四个核心按钮使用统一的字号/中心基线规则
```

中文目标字符：

```text
12
```

字体缺字：

```text
0
```

---

## 6. 图片 QA

8 张图片全部通过：

```text
尺寸变化 = 0
Alpha 变化 = 0
允许区域外变化 = 0
284x84 按钮区之外变化 = 0
errors = 0
```

其中 `0003` 的 672x672 atlas 也通过区域外零改动检查。

---

## 7. v007 构建

目录：

```text
05_build/v007-title-core/
```

测试包：

```text
05_build/v007-title-core/v007-title-core-test.zip
```

继承自 v006a 的文件：

```text
32
```

逐文件 SHA-256 对比结果：

```text
继承文件变化 = 0
```

新增标题 PNG：

```text
8
```

最终 ZIP：

```text
files       = 40
PNG         = 25
title PNG   = 8
size        = 8,410,874 bytes
```

SHA-256：

```text
EC1957F6B5641CDBD5457013B17096C7474FEFBBB191A9FA86684A95D1E1EF70
```

危险文件检查继续保持 0：

```text
NSP / NSZ / NCA / prod.keys / title.keys
```

---

## 8. 实机测试重点

标题页优先确认：

```text
从头开始
继续
环境设置
鉴赏
```

检查普通态与选中态切换时是否都保持中文。

`0005～0009` 对应的后续/解锁菜单暂未修改，如果实机出现这些项目仍为日文属于当前已知安全保留项，不应误判为 v007 回退。

下一阶段继续解析：

```text
System/systemmenu.png
System/systemmenu_a.png
System/systemmenu.spm
```

优先绑定并汉化游戏内系统菜单的保存 / 读取 / 环境设置等高频功能项。
