# 《永恒少女》NS 汉化阶段 15：v013 完整 1080 与增量字体

## 1. 基线

从：

```text
05_build/v012-scene1070/
```

继续推进，新增完整 `1080` 场景。

---

## 2. 1080 对齐审计

v2 统计：

```text
NS 正文          57
PC 正文          59
已配对           55
unmatched NS      2
PC-only           4
high-candidate   51
review            4
```

局部拆分差异集中在场景开头。

最终显式 override：

```text
NS 7 -> PC 8   阿尔艾特内心台词
NS 8 -> PC 10  阿尔艾特内心台词
NS 9 -> PC 11  叙述
```

其中 NS 9 原先被 v2 错配到 PC 8，已人工纠正。

PC 7 / 9 是独立角色名“阿尔艾特”，NS 不保存对应独立记录。

---

## 3. 完整批次

生成脚本：

```text
04_scripts/make_scene1080_full_batch.py
```

最终批次：

```text
03_text/translated/scene1080-full-v1.tsv
```

统计：

```text
rows              = 57
voice rows         = 31
manual overrides   = 3
manual polishes    = 18
```

结构 QA：

```text
索引 0～56 连续
voice mismatch = 0
translation kana = 0
PC markup residue = 0
```

---

## 4. 人工翻译修正

除了结构 override，本场对旧 PC 译文做了 18 处固定润色。

主要修正：

```text
行き倒れの人間
```

旧 PC 译为“走投无路的人”，含义不准确；Switch 版修正为：

```text
一个倒在路边的人
```

同时统一学园名：

```text
プエラリウム -> 普埃拉利姆
```

移除 PC 版附带的全角英文 ruby：

```text
Ｐｏｕｅｌｌａｒｉｕｍ
```

本场最终中文译文中的 `@r` ruby：

```text
0
```

另外修正“是的……”等不符合 `そう……` 语气的旧译，以及若干生硬叙述。

---

## 5. 累积文本与字体

v013 累积：

```text
v012-cumulative.tsv    1313
scene1080-full-v1.tsv    57
---------------------------
总计                   1370
```

累计目标字符：

```text
2494
```

字体新增码点：

```text
dfgotp5.ttc   6
dfhsr4.ttc    6
dfmrg5_u.ttc  5
```

3 TTC × 3 face：

```text
missing = 0
```

---

## 6. 写入与重提取

写入：

```text
files_written   = 1
strings_written = 57
```

最终：

```text
scene1080_reextract      = 57 / 57
voice_prefix_mismatch    = 0
scene_translation_kana   = 0
```

---

## 7. v013 测试包

目录：

```text
05_build/v013-scene1080/
```

ZIP：

```text
05_build/v013-scene1080/v013-scene1080-test.zip
```

继承 v012：

```text
previous payload files = 46
unchanged              = 43
changed TTC            = 3
```

最终：

```text
files   = 47
PNG     = 27
BINU8   = 12
size    = 8,674,777 bytes
```

SHA-256：

```text
491E3D93A3B5B8529A9437D1F232B6162F4AB8C2D89D064F9D3B05F330E5AB5B
```

危险文件：

```text
NSP       0
NSZ       0
NCA       0
prod.keys 0
title.keys 0
```

最终：

```text
errors = 0
```

---

## 8. 实机测试重点

```text
钟塔开场两条内心独白
“普埃拉利姆”名称显示
钟声毁灭传说相关对话
露珂与阿尔艾特的长段交流
“倒在路边的人”修正行
```

静态 QA 已全部通过；实机反馈继续单独记录。
