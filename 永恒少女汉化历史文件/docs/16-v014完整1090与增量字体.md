# 《永恒少女》NS 汉化阶段 16：v014 完整 1090 与增量字体

## 1. 基线

从 `05_build/v013-scene1080/` 继续，新增完整 `1090` 场景。

## 2. 对齐审计

```text
NS 正文          30
PC 正文          31
已配对           29
unmatched NS      1
PC-only           2
high-candidate   27
review            2
```

唯一结构 override：

```text
NS 4 -> PC 5  （……「阿薇拉」）
```

PC 4 为独立角色名“阿尔艾特”，NS 不保留对应记录。

## 3. 完整批次

```text
04_scripts/make_scene1090_full_batch.py
03_text/translated/scene1090-full-v1.tsv
```

结果：

```text
rows              = 30
voice rows         = 10
manual overrides   = 1
manual polishes    = 13
```

QA：

```text
索引 0～29 连续
voice mismatch = 0
translation kana = 0
PC markup residue = 0
```

## 4. 人工翻译修正

主要修正两处明显旧译错误。

```text
ガラスドームの中にあえかな光が灯った
```

修正为：

```text
看到玻璃罩内亮起一缕微弱的光，阿尔艾特停下了脚步。
```

以及：

```text
視線に意思と同じだけの熱が宿るなら
```

修正为：

```text
如果那道视线真拥有与其中怒意同等的热度，
阿尔艾特恐怕早在这一瞬间被烧成焦炭了。
```

同时修正 `世話係` 为“负责照看你的人”，以及叙述人称泄漏、残句和阿薇拉部分台词语气。

## 5. 累积与字体

```text
v013-cumulative.tsv    1370
scene1090-full-v1.tsv    30
---------------------------
总计                   1400
```

累计目标字符：

```text
2516
```

字体新增：

```text
dfgotp5.ttc   6
dfhsr4.ttc    6
dfmrg5_u.ttc  6
```

全部 9 个 face 缺字 0。

## 6. v014 测试包

```text
05_build/v014-scene1090/v014-scene1090-test.zip
```

统计：

```text
scene1090_reextract = 30 / 30
previous files      = 47
unchanged           = 44
changed TTC         = 3
files               = 48
PNG                 = 27
BINU8               = 13
size                = 8,681,715 bytes
errors              = 0
```

SHA-256：

```text
611BD3C7C65C5D6443BB0D23AB80A949054ED3855655D40743FDA3B3ECF521E7
```

危险文件检查：NSP / NSZ / NCA / prod.keys / title.keys 均为 0。

静态 QA 已通过，实机反馈继续单独记录。
