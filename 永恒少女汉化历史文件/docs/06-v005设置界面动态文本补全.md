# 《永恒少女》NS 汉化阶段 06：v005 设置界面动态文本补全

## 1. 实机反馈

用户对 v004 的实机反馈：

```text
多个设置界面没有完全汉化
其他测试没有问题
```

因此 v004 可以确认：

```text
1010 / 1020 / 1030 剧情链正常
语音正常
存档/读取正常
已有动态 UI 回写正常
新增事件/回想/鉴赏等功能说明正常
```

当前主要缺口集中到设置界面覆盖率。

---

## 2. 缺口审计

对 v004 已翻译集合与 UI 候选表做差集后，发现：

```text
UI 候选总数：546
v004 后仍未覆盖：420
```

其中最主要来源：

```text
Config/button.datu8       189 条实际记录
Config/event.datu8        152
Config/key.datu8           17
Config/system.datu8        16
Config/eventtype.datu8     12
Config/fontcolor.datu8     11
Config/buttonex.datu8      10
```

`button.datu8` 的剩余内容大量属于设置页可见说明，包括：

```text
显示/隐藏类开关
自动保存
保存/读取确认
快速设置
保存面板
自动/跳过模式
已读/等待图标
菜单背景
演出速度
字体类型/渐变/粗细/描边/阴影
角色文字颜色
样例消息
BGM / SE / Voice / System Voice
音量与测试播放
```

因此 v004 的问题确认是“覆盖范围不足”，不是 DATU8 回写失效。

---

## 3. v005 设置补全批次

生成脚本：

```text
04_scripts/make_ui_settings_complete_batch.py
```

批次：

```text
03_text/translated/ui-settings-complete-v1.tsv
```

新增写入：

```text
Config/button.datu8       189
Config/buttonex.datu8      10
Config/fontcolor.datu8     11
Config/system.datu8         7
Script/system.binu8         2
-----------------------------
合计                       219
```

短伪记录：

```text
.
1
4
L
:
```

保持不动，不作为 UI 文本处理。

本批还故意翻译了部分明显来自 PC 原版、Switch 通常不会显示的说明，例如：

```text
窗口模式
鼠标指针
滚轮速度
Twitter 上传
```

原因是这些内容已经存在于 Switch 资源中；即使某个隐藏配置入口意外暴露，也尽量避免出现日文。

---

## 4. v005 累积回写

累积表：

```text
03_text/translated/v005-cumulative.tsv
```

累计翻译记录：

```text
932
```

涉及 12 个文本资源：

```text
Config/button.datu8
Config/buttonex.datu8
Config/char.datu8
Config/fontcolor.datu8
Config/system.datu8
Script/eventmode.binu8
Script/replaymode.binu8
Script/scr1010.binu8
Script/scr1020.binu8
Script/scr1030.binu8
Script/system.binu8
Script/title.binu8
```

全部从干净 merged v1.0.3 RomFS 重新生成。

---

## 5. 写后 QA

独立重提取：

```text
05_build/v005-ui-complete/verify-reextract.tsv
```

结果：

```text
翻译记录：932
重新提取缺失：0
@vv 语音控制不一致：0
PC ^n / ruby 残留：0
```

设置相关重点资源的日文假名扫描：

```text
Config/button.datu8      0
Config/buttonex.datu8    0
Config/fontcolor.datu8   0
Config/system.datu8      0
Script/system.binu8      0
```

结构记录数：

```text
Config/buttonex.datu8    10  → 10
Config/char.datu8        12  → 12
Config/fontcolor.datu8   11  → 11
Config/system.datu8      90  → 90
eventmode.binu8           4  → 4
replaymode.binu8        178  → 178
scr1010.binu8            93  → 93
scr1020.binu8           254  → 254
scr1030.binu8           319  → 319
system.binu8            178  → 178
title.binu8             133  → 133
```

`Config/button.datu8` 的启发式扫描记录数仍会因重叠短伪记录变化；真正需要的中文翻译全部重新提取成功。

---

## 6. 字体

v005 译文字符：

```text
唯一字符：1591
非 ASCII：1556
```

source + translation 全集：

```text
检查字符：2229
missing_from_all_ttc：0
```

三个 TTC、全部 3 个 face：

```text
dfgotp5.ttc   [0, 0, 0]
dfhsr4.ttc    [0, 0, 0]
dfmrg5_u.ttc  [0, 0, 0]
```

字体缺字：0。

---

## 7. LayeredFS 测试包

构建目录：

```text
05_build/v005-ui-complete/
```

测试 ZIP：

```text
05_build/v005-ui-complete/v005-ui-complete-test.zip
```

包内文件：

```text
15
```

检查：

```text
whitelist_exact = True
NSZ / NSP / NCA / keys = 0
```

ZIP 大小：

```text
7,491,220 bytes
```

SHA-256：

```text
BAC39ED332663FFCAE8EE606C49A5741B23FA0277A4F37446E61FD031F412BA8
```

---

## 8. 本版实机目的

v005 不增加新剧情场景，专门用于确认设置 UI 动态文本是否已经补全。

重点逐页检查：

```text
ADV
系统
显示
消息 1
消息 2
声音
语音
字体/文字样式
自动保存/确认窗口
快速设置
保存面板
```

如果 v005 中仍看到日文，而帮助/说明文字已经是中文，则基本可以判定剩余日文来自：

```text
System/configmenu_0001.png ～ configmenu_0007.png
System/configmenubutton*.png
System/configbackground.png
```

这些属于图片烘焙 UI，下一阶段将按图片层单独处理。

---

## 9. 图片层现状

已经确认设置图片资源族：

```text
configbackground.png
configmenu_0001.png ～ configmenu_0007.png
configmenubutton.png
configmenubutton_0001.png ～ 0012_2.png
quickconfig.png
```

因此正式版的设置界面验收标准仍然是：

```text
动态文字无日文
图片烘焙功能标签无未登记日文
所有开关/滑条/页签仍正常操作
```
