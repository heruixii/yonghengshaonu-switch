# 《永恒少女》NS 汉化阶段 02：v001 实机通过与完整 1010 构建

## 1. v001-minimal 实机结论

用户已完成 Switch v1.0.3 实机测试，并确认：

```text
启动正常
中文显示正常
字体正常
语音正常
文本推进正常
存档/读档正常
补丁路径与 LayeredFS 覆盖正常
```

因此项目的第一道实机闸门正式通过。

这意味着以下底层技术链已获得真机验证：

```text
v1.0.3 BKTR 基底
BINU8 变长 UTF-8 回写
@vv 语音控制保留
@n 换行
中文 TTC 字形补充
Atmosphère LayeredFS 覆盖
```

---

## 2. v002 目标

在 v001 仅汉化 1010 开场前 21 条的基础上，v002 扩大为：

```text
完整汉化 Script/scr1010.binu8 的剧情正文
```

不修改：

```text
SE / cutin / 资源标识
ReplayMode 后的编译器辅助字符串
未确认的内部提示
其他场景脚本
```

---

## 3. 1010 对齐情况

场景 1010 的 v2 对齐统计：

```text
high-candidate：65
review：1
unmatched-ns：1
```

唯一 review 为开场第一句，已经人工逐句确认 PC 中文与 NS 日文对应，因此纳入本批次。

唯一 unmatched-ns 为：

```text
最初に戻りました
```

这是 PC 正文中不存在的内部提示，本批次保持日文，不纳入正文汉化。

另加入已在 v001 真机验证通过的说话人栏：

```text
私 → 我
```

最终 v002 场景批次：

```text
正文：66 条
人工确认说话人栏：1 条
总写入：67 条
```

其中：

```text
带 @vv 语音：15 条
带 @n 换行：7 条
带 NS ruby：1 条
```

批次文件：

```text
03_text/translated/scene1010-full-v1.tsv
```

---

## 4. BINU8 回写验证

完整 1010 回写后重新独立提取：

```text
原 scr1010 可提取记录：93
修改后可提取记录：93
写入：67
重新提取命中：67 / 67
缺失：0
语音控制不一致：0
PC ^n / PC ruby 残留：0
```

因此完整 1010 的静态脚本结构验证通过。

---

## 5. v002 字体

完整 1010 批次 source + translation 共涉及：

```text
检查字符：778
译文非 ASCII 字符：545
```

三个实际剧情 TTC 均有 3 个 face。

扩充后检查：

```text
dfgotp5.ttc   missing_per_face = [0, 0, 0]
dfhsr4.ttc    missing_per_face = [0, 0, 0]
dfmrg5_u.ttc  missing_per_face = [0, 0, 0]
```

完整 1010 字体缺字：0。

---

## 6. v002 LayeredFS

构建目录：

```text
05_build/v002-scene1010/
```

测试 ZIP：

```text
05_build/v002-scene1010/v002-scene1010-test.zip
```

LayeredFS 只包含 4 个文件：

```text
Script/scr1010.binu8
System/dfgotp5.ttc
System/dfhsr4.ttc
System/dfmrg5_u.ttc
```

白名单：

```text
whitelist_exact = True
```

禁止内容：

```text
NSZ / NSP / NCA / keys = 0
```

ZIP SHA-256：

```text
A722312AF22A96E5292E993F745EDA5B04C878A29152A5128C4DDF189319C2F5
```

---

## 7. v002 实机结果

用户已完成 v002 真机测试，并确认“没问题”。因此以下项目视为通过：

```text
完整 1010 能否一直推进到场景结束
后半段祷告对白语音是否同步
PC ruby 转换后的“永恒少女 / Ever Maiden”显示是否正常
长旁白换行是否溢出
从 1010 跳转到下一场景是否正常
```

v002 实机闸门：PASS。

下一阶段不再只推进剧情。根据用户明确要求，UI / 界面按钮与剧情并行汉化，且 UI 完整度列为正式发布硬门槛。

下一阶段包括：

```text
连续主线场景
人物名和术语统一
标题菜单
系统菜单
设置和按钮帮助
存档/读档
常驻按钮、自动/跳过/历史记录
回想/事件/CG/音乐/角色鉴赏
图片中烘焙的功能性日文
```

