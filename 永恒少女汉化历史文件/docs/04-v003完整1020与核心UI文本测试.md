# 《永恒少女》NS 汉化阶段 04：v003 完整 1020 + 核心 UI 文本测试

## 1. 前置结论

用户已确认：

```text
v001-minimal：实机正常
v002-scene1010：实机正常
```

因此 BINU8 变长 UTF-8 回写、中文 TTC、语音控制与 LayeredFS 技术链继续视为通过。

v003 首次加入 DATU8 UI 文本修改，因此本版本同时承担第一道 DATU8/UI 实机闸门。

---

## 2. 1020 场景

v2 对齐统计：

```text
NS 正文配对：222
high-candidate：218
review：4
pc-only：2
unmatched-ns：0
```

4 个 review 已逐项复核。

其中两处是 PC 把一条 NS 文本拆成两条：

```text
NS 45：PC 64 + 65
NS 132：PC 184 + 185
```

已人工合并并保留 NS 控制码：

```text
@n
@p
```

PC 正文中偶发的角色名“罗宾”统一修正为人物表固定译名“萝宾”。

最终：

```text
Script/scr1020.binu8 写入 222 条
带 @vv 语音文本：89 条
```

批次：

```text
03_text/translated/scene1020-full-v1.tsv
```

---

## 3. 核心人物名和术语

建立：

```text
03_text/translated/core-glossary-v1.tsv
```

首批人物名固定为：

```text
アルエット → 阿尔艾特
ルク       → 露珂
マコー     → 玛可
パヴォーネ → 帕沃妮
ロビン     → 萝宾
キャナリー → 卡娜莉
アヴェルラ → 阿薇拉
オルロ     → 奥尔洛
アドラー   → 阿德拉
その他     → 其他
ナレーション → 旁白
```

---

## 4. 第一批核心 UI 文本

生成脚本：

```text
04_scripts/make_ui_core_batch.py
```

批次：

```text
03_text/translated/ui-core-v1.tsv
```

只采用精确 source 映射，不使用模糊替换。

写入统计：

```text
Config/button.datu8   68
Config/char.datu8     11
Script/system.binu8    6
Script/title.binu8     1
合计                  86
```

当前覆盖的功能包括：

```text
人物显示名
上/下滚动
面板固定/解除
关闭窗口/菜单
TIPS
播放语音
上一/下一选项
历史记录
自动模式
跳过模式
保存
读取
系统菜单
设置菜单
返回标题
退出游戏
文本速度
窗口透明度
语音速度
主音量
保存数据移动/删除/备注
设置分类导航
标题画面系统提示
章节选择与第1～4章
```

注意：v003 处理的是动态 UI 文本和按钮帮助；`System/title_btn*.png`、`configmenu*.png` 等图片内烘焙文字尚未加入本测试包，待 DATU8/UI 实机闸门通过后继续处理。

---

## 5. v003 累积回写

累积表：

```text
03_text/translated/v003-cumulative.tsv
```

包含：

```text
1010：67
1020：222
UI：86
总计：375
```

涉及 6 个文本资源：

```text
Config/button.datu8
Config/char.datu8
Script/scr1010.binu8
Script/scr1020.binu8
Script/system.binu8
Script/title.binu8
```

全部从干净的 merged v1.0.3 RomFS 重新生成，不在旧补丁文件上继续叠改。

---

## 6. 写后重提取 QA

重新提取文件：

```text
05_build/v003-opening-ui/verify-reextract.tsv
```

结果：

```text
375 / 375 译文重新提取命中
缺失译文：0
语音 @vv 不一致：0
PC ^n 残留：0
PC ruby 残留：0
```

主要文件扫描记录数：

```text
Config/char.datu8      12 → 12
scr1010.binu8          93 → 93
scr1020.binu8         254 → 254
system.binu8          178 → 178
title.binu8           133 → 133
```

`Config/button.datu8` 的逐字节启发式扫描结果为：

```text
314 → 304
```

这 10 条差异已确认全部来自提取器的重叠短伪记录：

```text
"1" 少 6 条
"4" 少 2 条
"." 少 2 条
合计 10
```

真正写入的 68 条按钮/UI 中文全部重新提取成功，因此不是实际 UI 记录丢失。

---

## 7. v003 字体

累积 source + translation 字符：

```text
检查字符：1712
missing_from_all_ttc：0
```

译文唯一字符：

```text
1213
其中非 ASCII：1189
```

三个 TTC 均为 3 faces，最终检查：

```text
dfgotp5.ttc   [0, 0, 0]
dfhsr4.ttc    [0, 0, 0]
dfmrg5_u.ttc  [0, 0, 0]
```

字体缺字：0。

字体审计：

```text
05_build/v003-opening-ui/font-coverage.tsv
```

---

## 8. v003 LayeredFS

构建目录：

```text
05_build/v003-opening-ui/
```

测试 ZIP：

```text
05_build/v003-opening-ui/v003-opening-ui-test.zip
```

LayeredFS 使用本体 Title ID：

```text
01008DC019F7A000
```

包内白名单共 9 个文件：

```text
Config/button.datu8
Config/char.datu8
Script/scr1010.binu8
Script/scr1020.binu8
Script/system.binu8
Script/title.binu8
System/dfgotp5.ttc
System/dfhsr4.ttc
System/dfmrg5_u.ttc
```

检查：

```text
patch_file_count = 9
whitelist_exact = True
NSZ / NSP / NCA / keys = 0
```

ZIP 大小：

```text
7,276,901 bytes
```

SHA-256：

```text
F5E7574791CCC826C8225D7CC2430D20CCF498B0708FA427B83751F72DDDB41A
```

---

## 9. v003 实机重点

除完整 1020 剧情外，本版尤其检查：

```text
人物名是否正常显示中文
按钮帮助是否正常显示中文
打开/关闭系统菜单是否正常
自动/跳过/历史记录相关提示是否正常
保存/读取/设置相关按钮说明是否正常
章节选择提示与第1～4章是否正常
标题画面中的“无法保存”等提示是否正常
所有 UI 操作是否仍可正常点击和返回
存档/读取功能是否正常
```

若以上通过，则 DATU8 变长回写可进入扩大阶段，并开始加入图片型 UI 汉化。
